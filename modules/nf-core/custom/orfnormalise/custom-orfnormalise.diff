Changes in component 'nf-core/custom/orfnormalise'
'modules/nf-core/custom/orfnormalise/meta.yml' is unchanged
'modules/nf-core/custom/orfnormalise/main.nf' is unchanged
'modules/nf-core/custom/orfnormalise/environment.yml' is unchanged
Changes in 'custom/orfnormalise/templates/orfnormalise.py':
--- modules/nf-core/custom/orfnormalise/templates/orfnormalise.py
+++ modules/nf-core/custom/orfnormalise/templates/orfnormalise.py
@@ -766,6 +766,25 @@
     return clamp1000(min(-math.log10(p) * 100, 1000))
 
 
+def _price_transcript_id(orf_id_raw, orf_type):
+    """Recover the transcript id from PRICE's `Id` column.
+
+    PRICE builds `Id` as `<transcript_id>_<orf_type>_<n>` (an increasing
+    counter within the transcript). Anchoring the split on the row's own
+    `orf_type` value tolerates transcript ids that themselves contain
+    underscores (true for some non-Ensembl annotations), rather than
+    assuming the first or last underscore is the delimiter.
+    """
+    if orf_type:
+        m = re.match(r"^(.+)_" + re.escape(orf_type) + r"_\\d+\$", orf_id_raw)
+        if m:
+            return m.group(1)
+    parts = orf_id_raw.rsplit("_", 2)
+    if len(parts) == 3 and parts[2].isdigit():
+        return parts[0]
+    return orf_id_raw.split("_", 1)[0]
+
+
 def parse_price(path, transcripts, fields):
     resolved = {"score": "", "orf_type": ""}
     rows = []
@@ -785,12 +804,16 @@
 
             orf_type = (pick(row, fields, resolved, "orf_type") or "").strip()
 
-            tid = orf_id_raw.split("_", 1)[0]
-            gid = (row.get("Gene") or "").strip()
-            if not gid:
-                t = transcripts.get(tid)
-                if t is not None:
-                    gid = t.gene_id
+            tid = _price_transcript_id(orf_id_raw, orf_type)
+            t = transcripts.get(tid)
+            if t is not None:
+                gid = t.gene_id
+            else:
+                # PRICE's `Gene` column concatenates every gene overlapping the
+                # ORF's genomic span with an underscore, so this fallback value
+                # may itself be a multi-gene string; passed through as-is,
+                # downstream joins against a single-gene matrix won't match it.
+                gid = (row.get("Gene") or "").strip()
 
             length_nt = sum(b[1] - b[0] for b in blocks)
             aa_len = max(0, (length_nt - 3) // 3) if length_nt > 0 else 0

'modules/nf-core/custom/orfnormalise/tests/price_gene_precedence.gtf' was created
Changes in 'custom/orfnormalise/tests/main.nf.test':
--- modules/nf-core/custom/orfnormalise/tests/main.nf.test
+++ modules/nf-core/custom/orfnormalise/tests/main.nf.test
@@ -224,6 +224,42 @@
         }
     }
 
+    test("price - gene_id resolved from transcript, not the concatenated Gene column") {
+
+        when {
+            process {
+                """
+                input[0] = channel.of([
+                    [id: 'sample1'],
+                    file("\${projectDir}/modules/nf-core/custom/orfnormalise/tests/price_gene_precedence.orfs.tsv", checkIfExists: true),
+                    'price'
+                ])
+                input[1] = channel.of([
+                    [id: 'reference'],
+                    file("\${projectDir}/modules/nf-core/custom/orfnormalise/tests/price_gene_precedence.gtf", checkIfExists: true)
+                ])
+                """
+            }
+        }
+
+        then {
+            def lines = path(process.out.tsv[0][1]).text.readLines().findAll { !it.startsWith('#') }
+            def cols  = lines[0].split('\t') as List
+            def row   = lines[1].split('\t')
+            def gid   = cols.indexOf('gene_id')
+            def tid   = cols.indexOf('transcript_id')
+            assertAll(
+                { assert process.success },
+                { assert snapshot(process.out).match() },
+                // Gene column is 'ENSG_A_ENSG_B' (PRICE's concatenated form);
+                // the annotation resolves ENST_FAKE_TX to a single gene, which
+                // must win over the Gene column.
+                { assert row[gid] == 'ENSG_PLAIN' },
+                { assert row[tid] == 'ENST_FAKE_TX' }
+            )
+        }
+    }
+
     test("homo_sapiens [chr20] - ribotish - score-field override") {
         config "./score_override.config"
 

'modules/nf-core/custom/orfnormalise/tests/score_override.config' is unchanged
Changes in 'custom/orfnormalise/tests/main.nf.test.snap':
--- modules/nf-core/custom/orfnormalise/tests/main.nf.test.snap
+++ modules/nf-core/custom/orfnormalise/tests/main.nf.test.snap
@@ -243,5 +243,54 @@
             "nextflow": "25.10.4"
         },
         "timestamp": "2026-06-12T13:07:59.997149"
+    },
+    "price - gene_id resolved from transcript, not the concatenated Gene column": {
+        "content": [
+            {
+                "0": [
+                    [
+                        {
+                            "id": "sample1"
+                        },
+                        "sample1.normalised.bed12:md5,a41a522d29f027394cbc1bd9766b68eb"
+                    ]
+                ],
+                "1": [
+                    [
+                        {
+                            "id": "sample1"
+                        },
+                        "sample1.normalised.tsv:md5,aec9d0eec2ae9f5f9a13945e850bc241"
+                    ]
+                ],
+                "2": [
+                    "versions.yml:md5,e7fc396424c69f0969daec4a45c3de70"
+                ],
+                "bed12": [
+                    [
+                        {
+                            "id": "sample1"
+                        },
+                        "sample1.normalised.bed12:md5,a41a522d29f027394cbc1bd9766b68eb"
+                    ]
+                ],
+                "tsv": [
+                    [
+                        {
+                            "id": "sample1"
+                        },
+                        "sample1.normalised.tsv:md5,aec9d0eec2ae9f5f9a13945e850bc241"
+                    ]
+                ],
+                "versions": [
+                    "versions.yml:md5,e7fc396424c69f0969daec4a45c3de70"
+                ]
+            }
+        ],
+        "meta": {
+            "nf-test": "0.9.4",
+            "nextflow": "26.04.6"
+        },
+        "timestamp": "2026-07-27T09:50:45.960856891"
     }
 }
'modules/nf-core/custom/orfnormalise/tests/price_gene_precedence.orfs.tsv' was created
************************************************************
