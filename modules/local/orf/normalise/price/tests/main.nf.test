nextflow_process {

    name "Test Process ORF_NORMALISE_PRICE"
    script "../main.nf"
    process "ORF_NORMALISE_PRICE"

    tag "modules"
    tag "modules_local"
    tag "orf_normalise"
    tag "orf_normalise_price"

    test("synthetic data - stub") {
        options "-stub-run"

        when {
            process {
                """
                def orfs = file('allsamples.orfs.tsv')
                orfs.text = ""
                def gtf = file('reference.gtf')
                gtf.text = ""

                input[0] = Channel.of([ [id:'allsamples'], orfs ])
                input[1] = Channel.of([ [id:'reference'], gtf ])
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
